using UnityEngine;

public class Tot : MonoBehaviour {

    //public GameObject abgrund;

    //// Use this for initialization
    //void Start () {
    //    //totes bereich test
    //    this.transform.position = new Vector3(GameObject.FindGameObjectWithTag("Player").transform.position.x, -50, GameObject.FindGameObjectWithTag("Player").transform.position.z); //random spawn für cube

    //    abgrund = GameObject.CreatePrimitive(PrimitiveType.Cube);
    //    abgrund.transform.localScale = new Vector3(1000f, 0f, 1000f);
    //    abgrund.name = "Abgrund";
    //    abgrund.transform.position = this.transform.position;

    //    BoxCollider box = abgrund.GetComponent(typeof(BoxCollider)) as BoxCollider;    //aktiviert den collider im cube
    //    box.isTrigger = true;
    //}

    //void FixedUpdate()
    //{
    //    abgrund.transform.position = new Vector3(GameObject.FindGameObjectWithTag("Player").transform.position.x, -50, GameObject.FindGameObjectWithTag("Player").transform.position.z); //random spawn für cube
    //}
}
